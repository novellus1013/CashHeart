/// v2.0에서 임시로 꺼둔 기능. 관련 코드는 남겨두고(재구현 없이 재사용 예정)
/// 진입점만 숨긴다.
class FeatureFlags {
  /// 카드 공유(Sprint 4) — 카카오톡 등 메신저로 이미지+캡션 텍스트를 함께
  /// 보내면 캡션이 전달되지 않아(실기기 확인, 2026-07-11) QR을 이미지에
  /// 다시 넣었지만, "탭하면 바로 웹으로 이동"하는 경험을 위해서는 공유마다
  /// 개인화된 미리보기 이미지를 만들어주는 동적 OG 이미지 생성 백엔드가
  /// 필요하다고 판단해 v2.0에서는 보류하기로 확정(2026-07-11 사용자 결정).
  /// Flutter 쪽 구현(케이스 판정 `share_card_case.dart`, 정적 JSON 템플릿,
  /// 카드 UI, RepaintBoundary 캡처+공유)은 그대로 유지 — 백엔드가 생기면
  /// 캡처 방식만 "동적 URL 공유"로 바꾸면 재사용 가능. v2.1/v2.2에서 백엔드와
  /// 함께 재개할 때 이 플래그만 true로 바꾸면 된다.
  static const bool cardShareEnabled = false;
}
