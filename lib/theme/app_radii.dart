import 'package:flutter/material.dart';

/// design_handoff_cashheart 스펙의 모서리 반경 토큰.
class AppRadii {
  static const double card = 20.0;
  static const double tile = 22.0;
  static const double btn = 14.0;
  static const double sheet = 24.0;
  static const double pill = 999.0;

  static const cardRadius = BorderRadius.all(Radius.circular(card));
  static const tileRadius = BorderRadius.all(Radius.circular(tile));
  static const btnRadius = BorderRadius.all(Radius.circular(btn));
  static const sheetRadius = BorderRadius.all(Radius.circular(sheet));
  static const pillRadius = BorderRadius.all(Radius.circular(pill));
}
